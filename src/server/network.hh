#pragma once
#include "ll_network.hh"

// high level net code - if anything at this point can be called "high level"

namespace net {
        class connection
        {
                private:

                public:
                bool is_wizard;
        };
}
